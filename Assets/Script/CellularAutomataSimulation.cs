using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
///  
/// </summary>
public class CellularAutomataSimulation : MonoBehaviour
{
 

    // Update is called once per frame
    void Update()
    {
        
// // Step 1: 반응 (예: 마그마 + 물 -> 돌)
//         SimulateReactions(readBuffer, writeBuffer);
//         Swap(ref readBuffer, ref writeBuffer);
//
// // Step 2: 상태 변화 (예: 연소 중 -> 타버린 상태)
//         SimulateStateChanges(readBuffer, writeBuffer);
//         Swap(ref readBuffer, ref writeBuffer);
//
// // Step 3: 이동 (물 흐름, 가스 확산 등)
//         SimulateMovement(readBuffer, writeBuffer);
//         Swap(ref readBuffer, ref writeBuffer);
//
// // Step 4: 후처리 (플래그 클리어, 애니메이션 처리 등)
//         FinalizeStates(readBuffer, writeBuffer);
//         Swap(ref readBuffer, ref writeBuffer);
    }
}
